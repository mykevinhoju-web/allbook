import { SalonDashboardPlaceholder } from "@/features/dashboard";

export default function SalonCalendarPage() {
  return (
    <SalonDashboardPlaceholder
      title="Calendar"
      description="A full day/week/month schedule view will live here. The home widget already previews today’s slots."
    />
  );
}
