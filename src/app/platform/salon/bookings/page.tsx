import { SalonDashboardPlaceholder } from "@/features/dashboard";

export default function SalonBookingsPage() {
  return (
    <SalonDashboardPlaceholder
      title="Bookings"
      description="Manage appointments, statuses, and walk-ins. Booking logic will plug into this module next."
    />
  );
}
